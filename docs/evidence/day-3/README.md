# Day 3 evidence

Internship notebook attachments for Issue #3 (news item schema).

| File | What it shows |
|------|----------------|
| `news-item-schema.png` | Required vs optional fields and validation flow |

Optional: add your own screenshot of `poetry run pytest` green output as `pytest-day3.png`.
