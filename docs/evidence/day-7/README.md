# Day 7 evidence

| File | What it shows |
|------|----------------|
| `pytest-backoff.png` | Green pytest for backoff/retry tests |
| `backoff-delays.png` | (optional) printed delay sequence from a tiny demo |

Capture after: `poetry run pytest tests/unit/test_backoff.py tests/unit/test_retry.py -q`
