# Day 4 evidence

| File | What it shows |
|------|----------------|
| `scrapy-run-output.png` | (optional) terminal output of `poetry run python scripts/run_scrape.py` |
| `scraped-sample.jsonl.txt` | Sample JSONL lines produced by the demo spider |

Capture a terminal screenshot after a successful scrape and save it here for the internship notebook.
