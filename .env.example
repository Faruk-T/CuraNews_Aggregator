# Placeholder — copy to `.env` locally. Never commit real secrets.

APP_ENV=dev
LOG_LEVEL=INFO
APP_NAME=CuraNews Aggregator

DATABASE_URL=postgresql+psycopg://curanews:curanews@localhost:5432/curanews
REDIS_URL=redis://localhost:6379/0

SPACY_MODEL=en_core_web_sm
FEED_CACHE_TTL_SECONDS=120
SCRAPE_MAX_RETRIES=5
SCRAPE_BACKOFF_BASE=0.5
SCRAPE_CONCURRENCY=2

API_HOST=0.0.0.0
API_PORT=8000

# Override in local .env — do not use the default in shared environments
PII_HASH_SALT=change-me-in-local-only
